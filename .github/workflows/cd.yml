name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build Frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Build Backend
        working-directory: backend
        run: |
          chmod +x gradlew
          ./gradlew build

      - name: Deploy Railway
        run: npx @railway/cli up --service tetrisMaster
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
