{
  "name": "tetrisMaster",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd ../backend && ./gradlew clean bootJar"
  }
}
