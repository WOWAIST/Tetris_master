{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "chmod +x gradlew backend/gradlew && cd frontend && npm ci && npm run build && cd ../backend && ./gradlew clean bootJar --no-daemon --stacktrace"
  },
  "deploy": {
    "startCommand": "java -jar backend/build/libs/tetrisMaster-0.0.1-SNAPSHOT.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
