{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node --version && npm --version && java -version && chmod +x backend/gradlew && cd frontend && npm ci && npm run build && cd ../backend && ./gradlew clean bootJar"
  },
  "deploy": {
    "startCommand": "java -jar backend/build/libs/tetrisMaster-0.0.1-SNAPSHOT.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
